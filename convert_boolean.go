package main

import (
	"fmt"
)
//import "strconv"

func main() {
	isNew:= true
//	isNewString:= strconv.FormatBool(isNew)
//	fmt.Println("Your purchased confirmed is " + isNewString)
    fmt.Printf("%s %v", "Your Purchased confirmed is", isNew)
}
